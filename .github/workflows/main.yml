name: ci

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - main

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: setup env vars for PR
        if: github.event_name == 'pull_request'
        run: |
          echo "LALA=dev" >> $GITHUB_ENV
      - name: setup env vars for main
        if: github.event_name != 'pull_request'
        run: |
          echo "LALA=prod" >> $GITHUB_ENV
      - name: set env vars
        id: env-vars
        run: |
          echo "environment-name=${{ env.LALA }}" >> $GITHUB_OUTPUT

      - name: print envs
        run: |
          echo "${{ env.LALA }}"

  build:
    runs-on: ubuntu-latest
    needs:
      - setup
    steps:
      - uses: actions/checkout@v2
      - name: print vars
        run: |
          echo "${{ needs.setup.steps.env-vars.outputs.environment-name }}"
          echo "${{ needs.setup.steps.env-vars.outputs }}"
          echo "${{ needs.setup.env.LALA }}"
          echo "${{ needs.setup.env }}"
          echo "${{ env.LALA }}"
          echo "${{ LALA }}"
